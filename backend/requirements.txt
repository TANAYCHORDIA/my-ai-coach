# --- Core Backend ---
fastapi
uvicorn[standard]
pydantic
python-dotenv

# --- AI Engine (Gemini) ---
# Used in ai_engine.py for genai.GenerativeModel
google-generativeai

# --- Local RAG (SentenceTransformer) ---
# Used in ai_engine.py for the custom RAG pipeline
sentence-transformers
faiss-cpu
torch
numpy

# --- Helpers ---
# (pickle is built-in, no package needed)